#!/usr/bin/env node
// post-publish.mjs — Bing IndexNow + Google Search Console ping (PLACEHOLDER)
// Filled in execute Wave 5. IndexNow: POST sitemap URLs to api.indexnow.org
// (free, key file at /public/{KEY}.txt for ≥4 hosts). GSC: POST sitemap to
// Search Console API using GOOGLE_SEARCH_CONSOLE_TOKEN. Stubs out (exit 0
// + log) when token missing — RUNBOOK GSC section covers OAuth flow.
// Always exits 0.
process.exit(0);
