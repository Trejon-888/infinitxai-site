#!/usr/bin/env node
// publish-to-beehiiv.mjs — beehiiv newsletter publish (PLACEHOLDER)
// Filled in execute Wave 5. Reads BEEHIIV_API_KEY + BEEHIIV_PUBLICATION_ID;
// posts new MDX→draft via beehiiv v2 API. Stubs out (exit 0 + log) when
// either env var is missing — RUNBOOK BEEHIIV_KEYS section covers manual
// account creation + key fetch.
process.exit(0);
