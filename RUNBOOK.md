# INFINITX site RUNBOOK — PLACEHOLDER

Filled in execute Wave 6. Sections (per G6 grep):

- BEEHIIV — manual account creation + API key fetch
- CF_CUSTOM_DOMAIN — wrangler zone-attach + DNS verification
- GSC — Search Console OAuth + token persistence
- INDEXNOW — public/{KEY}.txt provisioning
- AUTOBUILD — GitHub Action wiring on main push
